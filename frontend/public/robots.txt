User-agent: *
Allow: /

# Sitemap
Sitemap: https://spotter-trip-planner.com/sitemap.xml

# Allow all search engines to crawl the site
# Block access to admin or sensitive areas if any
Disallow: /admin/
Disallow: /api/
Disallow: /*.json$

# Crawl delay (optional - only if server can't handle rapid requests)
# Crawl-delay: 1
